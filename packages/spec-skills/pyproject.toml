[project]
name = "spec-skills"
version = "0.1.0"
description = "Skills for specification development"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "common",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/spec_skills"]

[tool.uv.sources]
common = { workspace = true }
